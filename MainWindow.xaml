<Window x:Class="StreamLighter.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="StreamLighter" Width="420" Height="220" MinWidth="360" MinHeight="200" Topmost="True" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0" HorizontalAlignment="Left">
                <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">Effect:</TextBlock>
                <ToggleButton x:Name="ToggleOverlay" Width="80" Click="ToggleOverlay_Click">Off</ToggleButton>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">Monitor:</TextBlock>
                <ComboBox x:Name="ComboMonitors" Width="160" SelectionChanged="ComboMonitors_SelectionChanged" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">Brightness</TextBlock>
            <Slider x:Name="SliderBrightness" Minimum="0" Maximum="1" Value="0.9" Width="180" ValueChanged="SliderBrightness_ValueChanged" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,4,0,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,4">
                <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">Edges:</TextBlock>
                <CheckBox x:Name="ChkTop" Content="Top" IsChecked="True" Checked="FillOption_Changed" Unchecked="FillOption_Changed" Margin="4,0"/>
                <CheckBox x:Name="ChkBottom" Content="Bottom" IsChecked="True" Checked="FillOption_Changed" Unchecked="FillOption_Changed" Margin="4,0"/>
                <CheckBox x:Name="ChkLeft" Content="Left" IsChecked="True" Checked="FillOption_Changed" Unchecked="FillOption_Changed" Margin="4,0"/>
                <CheckBox x:Name="ChkRight" Content="Right" IsChecked="True" Checked="FillOption_Changed" Unchecked="FillOption_Changed" Margin="4,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">Thickness</TextBlock>
                <Slider x:Name="SliderThickness" Minimum="0" Maximum="600" Value="200" Width="180" ValueChanged="SliderThickness_ValueChanged" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Vertical">
            <TextBlock TextWrapping="Wrap" FontSize="11">Tip: Overlay is click-through so it won't block apps. Use the toggle to enable/disable.</TextBlock>
            <TextBlock HorizontalAlignment="Right" FontSize="10" Foreground="Gray" Margin="0,6,0,0">by NotUnrealEngineer</TextBlock>
        </StackPanel>
    </Grid>
</Window>
